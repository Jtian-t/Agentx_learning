# 身份校验实战计划 (第一阶段 - 原厂对齐版)

## 🎯 目标
完成 `AgentX` 原厂风格的轻量化身份验证，通过 `Interceptor` 拦截器配合 `JWT` 实现。

---

## 📝 任务列表

### 1. 基础设施搭建
- [x] **1.1 响应封装**: 实现 `org.xhy.infrastructure.common.Result`。
- [x] **1.2 工具类**: 实现 `org.xhy.infrastructure.utils.JwtUtils` (核心加密/解析)。
- [x] **1.3 工具类**: 实现 `org.xhy.infrastructure.utils.PasswordUtils` (原厂用于加盐加密密码)。 


### 2. 核心拦截器实现

[//]: # (- [ ] **2.1 拦截逻辑**: 实现 `org.xhy.infrastructure.interceptor.AuthInterceptor` &#40;原厂核心校验类&#41;。)
 原项目中没有这个拦截器. 有一个 AdminAuthInterceptor拦截器 /** 管理员权限拦截器 用于拦截需要管理员权限的请求 */

 和一个 UserAuthInterceptor拦截器  /** 用户鉴权拦截器 用于拦截需要鉴权的请求，验证用户身份并设置用户上下文 */
- [x] **2.1 拦截逻辑**: 实现 `org.xhy.infrastructure.auth.UserAuthInterceptor`
> **补充扩展：**
>
> `import jakarta.servlet.http.HttpServletRequest;`
> `import org.springframework.web.servlet.HandlerInterceptor;`
>
> 这些是需要依赖 spring-boot-starter-web 启动类...

- [ ] **2.2 拦截逻辑**: 实现 `org.xhy.infrastructure.interceptor.AdminAuthInterceptor` /** 管理员权限拦截器 用于拦截需要管理员权限的请求 */
> **补充扩展：**
> TODO暂时不实现这个功能 
> 
> 因为增加管理员拦截器要引入userdomainservice.而这个类设计的类有点多

- [x] **2.3 保存用户上下文** 实现'org.xhy.infrastructure.auth.UserContext'  用户上下文 用于存储当前线程的用户信息
  obsidian://open?vault=%E5%AE%9E%E4%B9%A0%E5%87%86%E5%A4%87&file=spring%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%2F%E6%8B%A6%E6%88%AA%E5%99%A8%2F01%20afterCompletion
- [x] **2.4 拦截器注册**: 编写 `org.xhy.infrastructure.config.WebMvcConfig` (挂载拦截器，配置跨域)。


### 3. 错误处理与全局配置
- [x] **3.1 要引进全局异常捕获,要对一些常见的异常进行捕获,并返回统一的错误信息**
- [x] **3.1.1 实体未找到异常捕获**:实现`org.xhy.infrastructure.exception.EntityNotFoundException`
- [x] **3.1.2 参数校验异常捕获**:实现`org.xhy.infrastructure.exception.ParamValidationException`
- [x] **3.1.3 业务异常捕获**:实现`org.xhy.infrastructure.exception.BusinessException`
- [x] **3.2 异常捕获**: 实现 `org.xhy.infrastructure.config.GlobalExceptionHandler`。


### 4. 业务入口与契约实现 (By Gemini: 按照 v2.12 深度图谱扫描重构)
- [x] **4.1 数据契约 (DTO) 族**:
    - [x] **LoginRequest**: `org.xhy.interfaces.dto.user.request.LoginRequest` | 职责: 封装并验证登录表单 | 关键注解: `@Data`, `@NotBlank`
    - [x] **RegisterRequest**: `org.xhy.interfaces.dto.user.request.RegisterRequest` | 职责: 封装并验证注册表单 | 关键注解: `@Email`, `@NotBlank`
    - [x] **GetCaptchaRequest**: `org.xhy.interfaces.dto.user.request.GetCaptchaRequest` | 职责: 图形验证码请求参数 | 关键注解: `@Data`
    - [x] **CaptchaResponse**: `org.xhy.interfaces.dto.user.response.CaptchaResponse` | 职责: 封装 UUID 与 Base64 图片
- [ ] **4.2 基础设施补全 (Infrastructure)**:
    - [x] **CaptchaUtils**: `org.xhy.infrastructure.verification.CaptchaUtils` | 职责: 图片验证码绘图逻辑。
    - [x] **EmailService**: `org.xhy.infrastructure.email.EmailService` | 职责: 负责发送验证码邮件 | 依赖: `JavaMailSender`
>**补充扩展: **
>  这个类中,需要加入这个依赖
>  <!-- Jakarta Mail -->
>        <dependency>
>           <groupId>org.eclipse.angus</groupId>
>            <artifactId>jakarta.mail</artifactId>
>            <version>2.0.2</version>
>        </dependency>
> 还需要在配置文件中,配置邮件验证码的相关信息
> ```xml
> mail:
>   smtp:
>     host: ${MAIL_SMTP_HOST:smtp.qq.com}
>     port: ${MAIL_SMTP_PORT:587}
>     username: ${MAIL_SMTP_USERNAME:}
>     password: ${MAIL_SMTP_PASSWORD:}  
>   verification:
>     template: ${MAIL_VERIFICATION_TEMPLATE:您的验证码是:%s，有效期10分钟，请勿泄露给他人。}
>     subject: ${MAIL_VERIFICATION_SUBJECT:AgentX - 邮箱验证码}
> ```
  
 - [x] **VerificationCodeService**: `org.xhy.infrastructure.verification.VerificationCodeService` | 职责: 统一验证码（图片/邮件）存取校验 | 依赖: `TimedCache` (Hutool)
>**补充扩展: **
> [x] **CodeStorage**: `org.xhy.infrastructure.verification.storage.CodeStorage` | 职责: 验证码存储接口 | 依赖: `TimedCache` (Hutool)
- [ ] **4.3 领域服务层 (Domain Service)**:
    - [ ] **UserDomainService**: `org.xhy.domain.user.service.UserDomainService` | 职责: 用户状态变更、密码匹配、查重 | 依赖: `UserRepository`, `PasswordUtils`
>**补充扩展: **
>  [x] **UserRepository** `org.xhy.domain.user.repository.UserRepository` | 职责: 用户仓储接口 | 继承 MyBatisPlusExtRepository<UserEntity>
>  [x] **而MyBatisPlusExtRepository<T> 继承BaseMapper<T>** 
> 
>  [x] **UserSettingsRepository** `org.xhy.domain.user.repository.UserSettingsRepository` | 职责: 用户设置仓储接口 | 继承 MyBatisPlusExtRepository<UserSettingsEntity>
>  [x] **需要补充UserSettingEntity** `com.xhy.domain.user.model.UserSettingsEntity` 用来管理用户设置,比如默认模型ID,默认模型名称,默认模型参数等,其中具体的内容还未实现

** 涉及的部分太大了,其中大部分先不实现,注释点**
- [ ] **AuthSettingDomainService**: `org.xhy.domain.auth.service.AuthSettingDomainService` | 职责: 认证开关管理 | 依赖: `AuthFeatureKey` (常量) 
>**补充扩展: **
>其中```java
>  public List<UserEntity> getByIds(List<String> userIds) {
>  if (userIds == null || userIds.isEmpty()) {
>  return new ArrayList<>();
>  }
>  return userRepository.selectByIds(userIds); //因为用的是这个selectByIds方法,原先的版本依赖不对
>     <!-- MyBatis Plus -->
><dependency>
><groupId>com.baomidou</groupId>
>     <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
>       <!--            <version>3.5.5</version>-->
>            <version>${mybatis-plus.version}</version> 3.5.11版本是符合要求的
>        </dependency>
>  }```

- [x] **4.4 应用编排层 (Application Service)**:
    - [x] **LoginAppService**: `org.xhy.application.user.service.LoginAppService` | 职责: 编排 Auth 全流程 | **前置依赖: UserDomainService, EmailService, VerificationCodeService, AuthSettingDomainService, JwtUtils**
    - ** 因为AuthSettingDomainService 中涉及的类太多,我在这个类中,对于这个授权应用的调用,都注释掉了,没有判断用户是否禁用的逻辑**
- [x] **4.5 入口控制器 (Controller)**:
    - [x] **LoginController**: `org.xhy.interfaces.api.portal.user.LoginController` | 职责: Auth 路由入口 | 依赖: `LoginAppService`
  >**补充扩展: **:
> 一些复杂的功能我先砍掉了


### 5. 持久化层支撑 (By Gemini)
- [x] **5.1 领域实体**: `org.xhy.domain.user.model.UserEntity` | 职责: 映射数据库用户表 | 依赖: `@TableName`
- [x] **5.2 数据库映射 (Mapper)**: `org.xhy.domain.user.repository.UserRepository` | 职责: DB 原子操作 | 依赖: `BaseMapper<UserEntity>`
- [x] **5.3 环境检查**: 在 `pom.xml` 中引入 `spring-boot-starter-validation` 以支持 DTO 中的 `@NotBlank` 等校验注解。

---

## 💡 历史笔记与感悟 (保留内容)
- **拦截器选择**: 原项目中没有 `AuthInterceptor`，实际上是 `UserAuthInterceptor`。
- **关于 Admin 鉴权**: TODO暂时不实现，因为涉及 `UserDomainService` 链条较长。
- **用户上下文**: 参考 Obsidian 笔记: obsidian://open?vault=%E5%AE%9E%E4%B9%A0%E5%87%86%E5%A4%87&file=spring%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%2F%E6%8B%A6%E6%88%AA%E5%99%A8%2F01%20afterCompletion
- **Validation 提醒**: 记得引入 `spring-boot-starter-validation` 依赖。 (此处已在 5.3 节完成)

---

## 💡 Agent 协作提示
- 我已确认为原厂采用 **拦截器方案** 而非传统的 Security Filter 方案。
- 每一项开始前，我会自动展示 `reference/AgentX` 下的对应源码。
