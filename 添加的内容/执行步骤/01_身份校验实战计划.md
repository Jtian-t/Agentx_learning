# 身份校验实战计划 (第一阶段 - 原厂对齐版)

## 🎯 目标
完成 `AgentX` 原厂风格的轻量化身份验证，通过 `Interceptor` 拦截器配合 `JWT` 实现。

---

## 📝 任务列表

### 1. 基础设施搭建
- [x] **1.1 响应封装**: 实现 `org.xhy.infrastructure.common.Result`。
- [x] **1.2 工具类**: 实现 `org.xhy.infrastructure.utils.JwtUtils` (核心加密/解析)。
- [x] **1.3 工具类**: 实现 `org.xhy.infrastructure.utils.PasswordUtils` (原厂用于加盐加密密码)。 


### 2. 核心拦截器实现

[//]: # (- [ ] **2.1 拦截逻辑**: 实现 `org.xhy.infrastructure.interceptor.AuthInterceptor` &#40;原厂核心校验类&#41;。)
 原项目中没有这个拦截器. 有一个 AdminAuthInterceptor拦截器 /** 管理员权限拦截器 用于拦截需要管理员权限的请求 */

 和一个 UserAuthInterceptor拦截器  /** 用户鉴权拦截器 用于拦截需要鉴权的请求，验证用户身份并设置用户上下文 */
- [x] **2.1 拦截逻辑**: 实现 `org.xhy.infrastructure.auth.UserAuthInterceptor`
> **补充扩展：**
>
> `import jakarta.servlet.http.HttpServletRequest;`
> `import org.springframework.web.servlet.HandlerInterceptor;`
>
> 这些是需要依赖 spring-boot-starter-web 启动类...


- [x] **2.2 拦截逻辑**: 实现 `org.xhy.infrastructure.interceptor.AdminAuthInterceptor` /** 管理员权限拦截器 用于拦截需要管理员权限的请求 */
> **补充扩展：**
> TODO暂时不实现这个功能 
> 
> 因为增加管理员拦截器要引入userdomainservice.而这个类设计的类有点多

- [x] **2.3 保存用户上下文** 实现'org.xhy.infrastructure.auth.UserContext'  用户上下文 用于存储当前线程的用户信息
  obsidian://open?vault=%E5%AE%9E%E4%B9%A0%E5%87%86%E5%A4%87&file=spring%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%2F%E6%8B%A6%E6%88%AA%E5%99%A8%2F01%20afterCompletion
- [x] **2.4 拦截器注册**: 编写 `org.xhy.infrastructure.config.WebMvcConfig` (挂载拦截器，配置跨域)。

### 3. 错误处理与全局配置
- [x] **3.1 要引进全局异常捕获,要对一些常见的异常进行捕获,并返回统一的错误信息**
- [x] **3.1.1 实体未找到异常捕获**:实现`org.xhy.infrastructure.exception.EntityNotFoundException`
- [x] **3.1.2 参数校验异常捕获**:实现`org.xhy.infrastructure.exception.ParamValidationException`
- [x] **3.1.3 业务异常捕获**:实现`org.xhy.infrastructure.exception.BusinessException`
- [x] **3.2 异常捕获**: 实现 `org.xhy.infrastructure.config.GlobalExceptionHandler`。

### 4. 业务入口与契约实现 (By Gemini: 按照 v2.6 标准重构)
- [ ] **4.1 数据契约 (DTO) 族**:
    - [x] **LoginRequest**: `org.xhy.interfaces.dto.user.request.LoginRequest` | 职责: 封装并验证登录表单 | 依赖: `@NotBlank` | 关键注解: `@Data`
    - [x] **RegisterRequest**: `org.xhy.interfaces.dto.user.request.RegisterRequest` | 职责: 封装并验证注册表单 | 依赖: `@Email`, `@NotBlank`
    - [x] **GetCaptchaRequest**: `org.xhy.interfaces.dto.user.request.GetCaptchaRequest` | 职责: 图形验证码请求参数 (原厂预留) | 关键注解: `@Data`
    - [x] **CaptchaResponse**: `org.xhy.interfaces.dto.user.response.CaptchaResponse` | 职责: 封装 UUID 与 Base64 图片
- [ ] **4.2 基础设施补全 (Utils)**:
    - [ ] **CaptchaUtils**: `org.xhy.infrastructure.verification.CaptchaUtils` | 职责: 核心验证码生成逻辑 (AWT 绘图) | 依赖: `CaptchaResult` (内部类)
- [ ] **4.3 应用编排 (AppService)**:
    - [ ] **LoginAppService**: `org.xhy.application.user.service.LoginAppService` | 职责: 编排登录注册、Token 生成逻辑 | 依赖: `JwtUtils`, `UserDomainService`, `CaptchaUtils`
- [ ] **4.4 入口控制器 (Controller)**:
    - [ ] **LoginController**: `org.xhy.interfaces.api.portal.user.LoginController` | 职责: 暴露 Auth 相关 HTTP 接口 | 依赖: `LoginAppService`, `Result`, `LoginRequest`, `RegisterRequest`
- [ ] **4.5 联调验证**: 使用 Postman 测试全链路。
> **By Gemini:** 
> 记得引入 `spring-boot-starter-validation` 依赖。 (此处已在 5.3 节完成)

### 5. 持久化层支撑 (By Gemini)
- [ ] **5.1 领域实体**: `org.xhy.domain.user.model.UserEntity` | 职责: 映射数据库用户表 | 依赖: `BaseEntity`, `@TableName`
- [ ] **5.2 数据库映射 (Mapper)**: `org.xhy.domain.user.repository.UserRepository` | 职责: 数据库原子操作 | 依赖: `BaseMapper<UserEntity>`
- [ ] **5.3 领域服务**: `org.xhy.domain.user.service.UserDomainService` | 职责: 处理用户相关领域逻辑 (密码匹配、唯一性校验) | 依赖: `UserRepository`, `PasswordUtils`
- [x] **5.3 环境检查**: 在 `pom.xml` 中引入 `spring-boot-starter-validation` 以支持 DTO 中的 `@NotBlank` 等校验注解。

---

## 💡 Agent 协作提示
- 我已确认为原厂采用 **拦截器方案** 而非传统的 Security Filter 方案。
- 每一项开始前，我会自动展示 `reference/AgentX` 下的对应源码。
