# 身份校验实战计划 (第一阶段 - 原厂对齐版)

## 🎯 目标
完成 `AgentX` 原厂风格的轻量化身份验证，通过 `Interceptor` 拦截器配合 `JWT` 实现。

---

## 📝 任务列表

### 1. 基础设施搭建
- [x] **1.1 响应封装**: 实现 `org.xhy.infrastructure.common.Result`。
- [x] **1.2 工具类**: 实现 `org.xhy.infrastructure.utils.JwtUtils` (核心加密/解析)。
- [x] **1.3 工具类**: 实现 `org.xhy.infrastructure.utils.PasswordUtils` (原厂用于加盐加密密码)。 

### 2. 核心拦截器实现
- [x] **2.1 拦截逻辑**: 实现 `org.xhy.infrastructure.auth.UserAuthInterceptor`。
- [x] **2.3 用户上下文**: 实现 `org.xhy.infrastructure.auth.UserContext`。
- [x] **2.4 拦截器注册**: 编写 `org.xhy.infrastructure.config.WebMvcConfig`。

### 3. 错误处理与全局配置
- [x] **3.1 异常定义**: 实现 `BusinessException`, `ParamValidationException`, `EntityNotFoundException`。
- [x] **3.2 全局处理**: 实现 `org.xhy.infrastructure.config.GlobalExceptionHandler`。

### 4. 业务入口与契约实现 (By Gemini: 按照 v2.12 深度图谱扫描重构)
- [x] **4.1 数据契约 (DTO) 族**:
    - [x] **LoginRequest**: `org.xhy.interfaces.dto.user.request.LoginRequest` | 职责: 封装并验证登录表单 | 关键注解: `@Data`, `@NotBlank`
    - [x] **RegisterRequest**: `org.xhy.interfaces.dto.user.request.RegisterRequest` | 职责: 封装并验证注册表单 | 关键注解: `@Email`, `@NotBlank`
    - [x] **GetCaptchaRequest**: `org.xhy.interfaces.dto.user.request.GetCaptchaRequest` | 职责: 图形验证码请求参数 | 关键注解: `@Data`
    - [x] **CaptchaResponse**: `org.xhy.interfaces.dto.user.response.CaptchaResponse` | 职责: 封装 UUID 与 Base64 图片
- [ ] **4.2 基础设施补全 (Infrastructure)**:
    - [x] **CaptchaUtils**: `org.xhy.infrastructure.verification.CaptchaUtils` | 职责: 图片验证码绘图逻辑。
    - [ ] **EmailService**: `org.xhy.infrastructure.email.EmailService` | 职责: 负责发送验证码邮件 | 依赖: `JavaMailSender`
    - [ ] **VerificationCodeService**: `org.xhy.infrastructure.verification.VerificationCodeService` | 职责: 统一验证码（图片/邮件）存取校验 | 依赖: `TimedCache` (Hutool)
- [ ] **4.3 领域服务层 (Domain Service)**:
    - [ ] **UserDomainService**: `org.xhy.domain.user.service.UserDomainService` | 职责: 用户状态变更、密码匹配、查重 | 依赖: `UserRepository`, `PasswordUtils`
    - [ ] **AuthSettingDomainService**: `org.xhy.domain.auth.service.AuthSettingDomainService` | 职责: 认证开关管理 | 依赖: `AuthFeatureKey` (常量)
- [ ] **4.4 应用编排层 (Application Service)**:
    - [ ] **LoginAppService**: `org.xhy.application.user.service.LoginAppService` | 职责: 编排 Auth 全流程 | **前置依赖: UserDomainService, EmailService, VerificationCodeService, AuthSettingDomainService, JwtUtils**
- [ ] **4.5 入口控制器 (Controller)**:
    - [x] **LoginController**: `org.xhy.interfaces.api.portal.user.LoginController` | 职责: Auth 路由入口 | 依赖: `LoginAppService`

### 5. 持久化层支撑 (By Gemini)
- [ ] **5.1 领域实体**: `org.xhy.domain.user.model.UserEntity` | 职责: 映射数据库用户表 | 依赖: `@TableName`
- [ ] **5.2 数据库映射 (Mapper)**: `org.xhy.domain.user.repository.UserRepository` | 职责: DB 原子操作 | 依赖: `BaseMapper<UserEntity>`
- [x] **5.3 环境检查**: 在 `pom.xml` 中引入 `spring-boot-starter-validation`。

---

## 💡 历史笔记与感悟 (保留内容)
- **拦截器选择**: 原项目中没有 `AuthInterceptor`，实际上是 `UserAuthInterceptor`。
- **关于 Admin 鉴权**: TODO暂时不实现，因为涉及 `UserDomainService` 链条较长。
- **用户上下文**: 参考 Obsidian 笔记: obsidian://open?vault=%E5%AE%9E%E4%B9%A0%E5%87%86%E5%A4%87&file=spring%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%2F%E6%8B%A6%E6%88%AA%E5%99%A8%2F01%20afterCompletion
- **Validation 提醒**: 记得引入 `spring-boot-starter-validation` 依赖。
