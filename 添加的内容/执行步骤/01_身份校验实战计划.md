# 身份校验实战计划 (第一阶段)

## 🎯 目标
完成从 `Result` 封装到 `JWT 登录` 的全链路后端开发，确保能够通过 Postman 或前端进行联调。

---

## 📝 任务列表

### 1. 基础设施搭建
- [ ] **1.1 响应封装**: 实现 `org.xhy.infrastructure.common.Result`。
- [ ] **1.2 常量定义**: 实现 `org.xhy.common.constant.AuthConstant` (参考原厂，存放 Token 密钥等)。
- [ ] **1.3 工具类**: 实现 `org.xhy.infrastructure.utils.JwtUtils`。

### 2. 领域层与安全对接
- [ ] **2.1 用户加载**: 实现 `org.xhy.domain.user.service.impl.UserDetailsServiceImpl`。
- [ ] **2.2 异常处理**: 实现 `org.xhy.infrastructure.exception.GlobalExceptionHandler`。

### 3. Security 核心配置
- [ ] **3.1 过滤器**: 实现 `org.xhy.infrastructure.security.JwtAuthenticationFilter`。
- [ ] **3.2 配置类**: 实现 `org.xhy.infrastructure.config.SecurityConfig` (含 CORS 跨域配置)。

### 4. 业务入口
- [ ] **4.1 认证接口**: 编写 `org.xhy.web.controller.AuthController` 登录/注册逻辑。
- [ ] **4.2 联调验证**: 使用 Postman 测试 Token 生成与接口拦截。

---

## 💡 Agent 协作提示
- 每一项开始前，我会自动搜索 `reference/AgentX` 下对应的类供你参考。
- 完成一项后，请告诉我，我将为你更新此 TODO 列表并同步 Git。
