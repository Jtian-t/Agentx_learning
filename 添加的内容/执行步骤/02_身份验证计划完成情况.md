# 01_身份验证计划完成情况审计报告

## 📅 审计日期: 2026-04-19
## 状态概览: 🟢 核心链路已通，🟡 细节待打磨，🔴 运行时依赖待配置

---

## 📊 模块化完成度比对

### Phase 1: 基础设施搭建 (100%)
- [x] **Result 封装**: `infrastructure.common.Result` 已就绪。
- [x] **JwtUtils**: `infrastructure.utils.JwtUtils` 已实现 HS256 加密逻辑。
- [x] **PasswordUtils**: `infrastructure.utils.PasswordUtils` 已实现 BCrypt 加密。

### Phase 2: 核心拦截器 (90%)
- [x] **UserAuthInterceptor**: 已实现身份校验逻辑。
- [x] **UserContext**: 已实现 ThreadLocal 存储用户信息。
- [x] **WebMvcConfig**: 已完成拦截器注册与跨域配置。
- [ ] **AdminAuthInterceptor**: 计划中已明确 **“暂时不实现”**，故不计入缺失。

### Phase 3: 全局异常处理 (100%)
- [x] **异常定义**: `BusinessException`, `EntityNotFoundException`, `ParamValidationException` 均已建立。
- [x] **GlobalExceptionHandler**: 已实现统一捕获并将异常转为 `Result` 格式输出。

### Phase 4: 业务入口与契约 (85%)
- [x] **DTO 族**: `LoginRequest`, `RegisterRequest`, `GetCaptchaRequest`, `CaptchaResponse` 均已实现。
- [x] **CaptchaUtils**: 验证码绘图逻辑已完成。
- [x] **EmailService**: 代码已编写（支持 SMTP 与 Jakarta Mail），但 **🔴 配置文件配置未落地**。
- [x] **VerificationCodeService**: 基于 `TimedCache` 的验证码存储与校验逻辑已完成。
- [x] **LoginAppService**: 业务编排逻辑已实现，但由于领域服务不完整，部分逻辑（如开关控制）被注释掉。
- [x] **LoginController**: 入口映射已对齐。

### Phase 5: 持久化支撑 (60%)
- [x] **UserEntity**: 实体类映射已完成。
- [x] **UserRepository**: Mapper 接口已定义，继承自 `MyBatisPlusExtRepository`。
- [ ] **MyBatis-Plus 配置**: **🔴 配置文件中缺失 Mapper 扫描路径**，导致启动时无法注入 Bean（见 Entry 07）。

---

## 🚩 关键偏差与技术债 (Critical Blockers)

1. **运行时阻碍 (Runtime Blocker)**: 
   - **MyBatis-Plus 扫描配置**: `application.yaml` 尚未配置 `mybatis-plus.mapper-locations` 及启动类的 `@MapperScan`。这是目前项目无法成功运行的最大障碍。
2. **业务逻辑缺口**:
   - **AuthSettingDomainService**: 逻辑目前被注释，导致“登录禁启用”等后台管理开关尚未生效。
   - **UserDomainService**: 目前处于“空壳”或“部分实现”状态，需要进一步补全。
3. **基础设施配置**:
   - **Email 账号**: `application.yaml` 中需要填入真实的 SMTP 授权码才能测试邮件功能。

---

## 🎯 后续动作建议 (Next Steps)

> **补充扩展 By Gemini:**
> 1. **首要修复**: 补全 `application.yaml` 的 MyBatis-Plus 与 Email 配置，确保项目能跑起来。
> 2. **解开封印**: 逐步解开 `LoginAppService` 中的注释逻辑，完善 `AuthSettingDomainService`。
> 3. **联调测试**: 使用 Postman 跑通 `getCaptcha` -> `sendEmail` -> `register` -> `login` 的全流程。

---

**审计结论**: 整体完成度约 **80%**。项目架构已成型，核心类均已落位，目前处于“万事俱备，只欠配置”的临界点。
